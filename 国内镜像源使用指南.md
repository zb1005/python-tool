# 国内Python包镜像源使用指南

## 常用镜像源列表

| 镜像源名称 | 镜像源地址 | 备注 |
|----------|-----------|------|
| 清华大学 | `https://pypi.tuna.tsinghua.edu.cn/simple/` | 推荐使用，更新及时 |
| 阿里云 | `https://mirrors.aliyun.com/pypi/simple/` | 速度快，稳定性好 |
| 豆瓣 | `https://pypi.douban.com/simple/` | 历史悠久，稳定 |
| 中科大 | `https://pypi.mirrors.ustc.edu.cn/simple/` | 学术机构，可靠 |
| 华为云 | `https://mirrors.huaweicloud.com/repository/pypi/simple/` | 企业级服务 |

## 使用方法

### 1. 临时使用（推荐）
在安装包时直接指定镜像源：
```bash
# 使用清华大学镜像源
python -m pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 使用阿里云镜像源
python -m pip install 包名 -i https://mirrors.aliyun.com/pypi/simple/

# 使用豆瓣镜像源
python -m pip install 包名 -i https://pypi.douban.com/simple/
```

### 2. 设置默认镜像源
创建pip配置文件（Windows系统）：

**方法一：用户级别配置**
在用户目录创建 `pip` 文件夹和 `pip.ini` 文件：
```ini
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple/
trusted-host = pypi.tuna.tsinghua.edu.cn

[install]
trusted-host = pypi.tuna.tsinghua.edu.cn
```

**方法二：项目级别配置**
在当前项目目录创建 `pip.conf` 文件（已创建）。

### 3. 命令行永久设置
```bash
# 设置默认镜像源
python -m pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple/

# 设置信任主机
python -m pip config set global.trusted-host pypi.tuna.tsinghua.edu.cn
```

## 常用包安装示例

```bash
# 安装数据分析包
python -m pip install pandas numpy matplotlib seaborn -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 安装网络请求包
python -m pip install requests beautifulsoup4 scrapy -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 安装机器学习包
python -m pip install scikit-learn tensorflow torch -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 安装Web开发包
python -m pip install flask django fastapi -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

## 验证镜像源是否生效

```bash
# 查看当前配置
python -m pip config list

# 测试安装速度
python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

## 注意事项

1. **信任主机设置**：部分镜像源需要设置 `trusted-host` 参数
2. **HTTPS支持**：确保镜像源支持HTTPS协议
3. **更新频率**：不同镜像源的更新频率可能不同
4. **备用方案**：如果某个镜像源不可用，可以切换到其他镜像源

## 故障排除

如果遇到连接问题，可以尝试：
1. 更换其他镜像源
2. 检查网络连接
3. 临时关闭防火墙或杀毒软件
4. 使用 `--trusted-host` 参数

## 推荐的镜像源组合

**日常使用**：清华大学镜像源（速度快，更新及时）
**备用方案**：阿里云镜像源（稳定性好）
**紧急情况**：豆瓣镜像源（历史悠久，可靠性高）